" Syntax Highlighting
syntax enable

" Ruler is in bottom bar, tells position
set ruler

" When enabled together, gives absolute and relative line
set relativenumber
set number

" Indentation settings
set tabstop=2
set expandtab
set autoindent

" jj is escape
imap jj <Esc>

" Menu pops up for wild card search
set wildmenu

" display matching bracket
set showmatch

" Show search results while typing, highlight results
set incsearch
set hlsearch

"code folding
set foldenable
